<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark parent so the window background behind Compose is dark too;
         otherwise the app flashes white while the activity starts. -->
    <style name="Theme.QNote" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
