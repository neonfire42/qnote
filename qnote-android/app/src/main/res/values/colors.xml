<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches `background` in Theme.kt, so the launch window and the
         Compose content are the same colour. -->
    <color name="window_background">#101418</color>
</resources>
